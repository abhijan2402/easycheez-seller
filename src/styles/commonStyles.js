import { StyleSheet } from "react-native";

export const commoneStyles = StyleSheet.create({
    textField: {
        borderWidth: 1,
        borderRadius: 8,
        borderColor: "#808080",
        marginHorizontal: 20,
        marginVertical: 10,
        color:"black",
        paddingHorizontal:15
    }
})